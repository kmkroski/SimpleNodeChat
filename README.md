SimpleNodeChat
==============

A demo chat web app using Node.js and sockets.IO

To run the server:
```bash
cd /PATH/TO/PROJECT/
node server.js
```

To view the webapp, open your browser and go to:
```bash
http://localhost:8080/
```
